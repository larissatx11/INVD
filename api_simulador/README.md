# API Simulador da Colmeia Inteligente

1. **Construir a imagem Docker**

sudo docker build -t api-colmeia .

2. **Rodar o container**

sudo docker run -p 8000:8000 api-colmeia

3. **Testar API**

http://localhost:8000/dados
